{
  "name": "scenario-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "OpenSCENARIO + OpenDRIVE → OSI-style ground truth, entirely in your browser.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  },
  "license": "MIT"
}
